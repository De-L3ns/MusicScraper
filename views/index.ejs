<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Music Scraper</title>
</head>
<body>
    <h1>Top 50 Ultratop</h1>

    <h2>Top 50 This week </h2>

    <ol>
        <% Object.keys(top50).forEach(function(key) { %>
            <li> <%= top50[key] %> - <%= key %> </li>
        <% }) %>
    </ol>
    
    <h2>Top 50 Last week</h2>

    <ol>
        <% Object.keys(top50prev).forEach(function(key) { %>
            <li> <%= top50prev[key] %> - <%= key %> </li>
        <% }) %>
    </ol>

        
</body>
<script src=script.js></script>
</html>